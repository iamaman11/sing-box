[package]
name = "edge-orchestrator"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
edge-controller-core = { path = "../edge-controller-core" }
serde = { version = "1.0.228", features = ["derive"] }

[lib]
path = "src/lib.rs"
