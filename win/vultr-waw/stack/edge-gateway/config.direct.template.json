{
  "log": {
    "level": "info",
    "timestamp": true
  },
  "dns": {
    "servers": [
      {
        "tag": "cloudflare",
        "type": "udp",
        "server": "1.1.1.1"
      },
      {
        "tag": "quad9",
        "type": "udp",
        "server": "9.9.9.9"
      }
    ],
    "final": "cloudflare",
    "strategy": "prefer_ipv4"
  },
  "inbounds": [
    {
      "type": "http",
      "tag": "http-proxy-auth",
      "listen": "::",
      "listen_port": 4128,
      "users": [
        {
          "username": "${PROXY_USERNAME}",
          "password": "${PROXY_PASSWORD}"
        }
      ]
    },
    {
      "type": "socks",
      "tag": "socks-proxy-auth",
      "listen": "::",
      "listen_port": 4080,
      "users": [
        {
          "username": "${PROXY_USERNAME}",
          "password": "${PROXY_PASSWORD}"
        }
      ]
    },
    {
      "type": "http",
      "tag": "https-proxy-auth",
      "listen": "::",
      "listen_port": 4443,
      "users": [
        {
          "username": "${PROXY_USERNAME}",
          "password": "${PROXY_PASSWORD}"
        }
      ],
      "tls": {
        "enabled": true,
        "certificate_path": "/certs/proxy.crt",
        "key_path": "/certs/proxy.key",
        "min_version": "1.2",
        "alpn": [
          "h2",
          "http/1.1"
        ]
      }
    }
  ],
  "outbounds": [
    {
      "type": "direct",
      "tag": "direct"
    },
    {
      "type": "block",
      "tag": "block"
    }
  ],
  "route": {
    "rules": [
      {
        "ip_is_private": true,
        "outbound": "block"
      }
    ],
    "final": "direct",
    "default_domain_resolver": "cloudflare"
  }
}
