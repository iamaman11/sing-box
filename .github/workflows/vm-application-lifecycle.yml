name: VM Application Lifecycle

on:
  workflow_call:
    inputs:
      command_body:
        description: Authorized /application command from the control-plane router
        required: true
        type: string

permissions: {}

jobs:
  authorize:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      operation: ${{ steps.parse.outputs.operation }}
      spec_path: ${{ steps.parse.outputs.spec_path }}
      rollback_digest: ${{ steps.parse.outputs.rollback_digest }}
      recovery_authority_digest: ${{ steps.parse.outputs.recovery_authority_digest }}

    steps:
      - name: Parse and authorize typed request
        id: parse
        shell: bash
        env:
          COMMENT_BODY: ${{ inputs.command_body }}
        run: |
          set -euo pipefail
          python3 <<'PY'
          import os
          import re

          spec_re = re.compile(r"^infra/application/[A-Za-z0-9._/-]+\.json$")
          digest_re = re.compile(r"^[0-9a-f]{64}$")

          def fail(message):
              raise SystemExit(message)

          tokens = os.environ.get("COMMENT_BODY", "").strip().split()
          operation = ""
          spec_path = ""
          rollback_digest = ""
          recovery_authority_digest = ""

          if tokens == ["/application", "acceptance"]:
              operation = "acceptance"
              spec_path = "infra/application/disposable-acceptance.json"
          elif tokens == ["/application", "cleanup"]:
              operation = "cleanup"
              spec_path = "infra/application/disposable-acceptance.json"
          elif len(tokens) == 3 and tokens[0] == "/application" and tokens[1] in {
              "plan", "apply", "verify", "upgrade", "recover-plan", "rollback-plan"
          }:
              operation = tokens[1]
              spec_path = tokens[2]
          elif (
              len(tokens) == 4
              and tokens[0] == "/application"
              and tokens[1] == "recover-apply"
          ):
              operation = "recover-apply"
              spec_path = tokens[2]
              recovery_authority_digest = tokens[3]
          elif (
              len(tokens) == 4
              and tokens[0] == "/application"
              and tokens[1] == "rollback-apply"
          ):
              operation = "rollback-apply"
              spec_path = tokens[2]
              rollback_digest = tokens[3]
          else:
              fail("unsupported /application command grammar")

          if not spec_re.fullmatch(spec_path) or ".." in spec_path or "//" in spec_path:
              fail("invalid application spec path")
          if operation == "rollback-apply":
              if not digest_re.fullmatch(rollback_digest):
                  fail("rollback-apply requires a lowercase 64-character digest")
          elif rollback_digest:
              fail("rollback digest is only valid for rollback-apply")

          if operation == "recover-apply":
              if not digest_re.fullmatch(recovery_authority_digest):
                  fail("recover-apply requires the exact recovery-plan authority digest")
          elif recovery_authority_digest:
              fail("recovery authority digest is only valid for recover-apply")

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as handle:
              handle.write(f"operation={operation}\n")
              handle.write(f"spec_path={spec_path}\n")
              handle.write(f"rollback_digest={rollback_digest}\n")
              handle.write(f"recovery_authority_digest={recovery_authority_digest}\n")
          PY

  execute:
    needs: authorize
    concurrency:
      group: vultr-control-plane-production
      cancel-in-progress: false
    if: needs.authorize.outputs.operation != 'acceptance' && needs.authorize.outputs.operation != 'cleanup'
    runs-on: ubuntu-24.04
    environment:
      name: production-vultr
      deployment: false
    timeout-minutes: 45
    permissions:
      contents: read
    env:
      REQUESTED_OPERATION: ${{ needs.authorize.outputs.operation }}
      SPEC_PATH: ${{ needs.authorize.outputs.spec_path }}
      ROLLBACK_DIGEST: ${{ needs.authorize.outputs.rollback_digest }}
      RECOVERY_AUTHORITY_DIGEST: ${{ needs.authorize.outputs.recovery_authority_digest }}

    steps:
      - name: Checkout exact canonical revision
        shell: bash
        env:
          REPOSITORY: ${{ github.repository }}
          REVISION: ${{ github.sha }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "https://github.com/${REPOSITORY}.git"
          git fetch --no-tags --depth=1 origin "${REVISION}"
          git checkout --detach FETCH_HEAD
          test "$(git rev-parse HEAD)" = "${REVISION}"
          test -f "${SPEC_PATH}"

      - name: Resolve exact durable Edge Platform release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          ACCEPTED_REVISION: ${{ github.sha }}
          OUTPUT_DIR: ${{ runner.temp }}/edge-platform-release
        run: |
          set -euo pipefail
          bash edge-platform/scripts/resolve_durable_release.sh

      - name: Materialize verified application artifacts
        shell: bash
        run: |
          set -euo pipefail
          dir="${RUNNER_TEMP}/edge-platform-release"
          source "${dir}/resolved.env"
          test "${EDGE_ACCEPTED_REVISION}" = "${GITHUB_SHA}"
          test "${EDGE_RELEASE_TAG}" = "edge-release-${EDGE_RELEASE_SET_SHA256}"
          test "${EDGE_RELEASE_SCHEMA_VERSION}" = "4"
          [[ "${EDGE_RUNTIME_SOURCE_REVISION}" =~ ^[0-9a-f]{40}$ ]]
          [[ "${EDGE_RUNTIME_INPUT_SHA256}" =~ ^[0-9a-f]{64}$ ]]
          orchestrator="${dir}/edge-orchestrator-linux-amd64"
          agent="${dir}/edge-agent-linux-amd64"
          test "$(sha256sum "${orchestrator}" | awk '{print $1}')" = "${EDGE_ORCHESTRATOR_SHA256}"
          test "$(sha256sum "${agent}" | awk '{print $1}')" = "${EDGE_AGENT_SHA256}"
          gateway_image="${EDGE_GATEWAY_IMAGE}"
          warp_image="${EDGE_WARP_EGRESS_IMAGE}"
          mesh_image="${EDGE_MESH_IMAGE}"
          [[ "${gateway_image}" =~ ^ghcr\.io/iamaman11/vultr-edge-gateway@sha256:[0-9a-f]{64}$ ]]
          [[ "${warp_image}" =~ ^ghcr\.io/iamaman11/vultr-warp-egress@sha256:[0-9a-f]{64}$ ]]
          [[ "${mesh_image}" =~ ^docker\.io/cloudflare/mesh@sha256:[0-9a-f]{64}$ ]]
          bundle_root="$(jq -er '.bundle_root' "${SPEC_PATH}")"
          test -d "${bundle_root}"
          printf 'EDGE_GATEWAY_IMAGE=%s\nEDGE_WARP_EGRESS_IMAGE=%s\nCLOUDFLARE_MESH_IMAGE=%s\n' \
            "${gateway_image}" "${warp_image}" "${mesh_image}" > "${bundle_root}/.images.env"
          chmod 0644 "${bundle_root}/.images.env"
          chmod 0755 "${orchestrator}" "${agent}"
          printf 'EDGE_DOCKER_ENGINE_VERSION=%s\n' "${EDGE_DOCKER_ENGINE_VERSION}" >> "${GITHUB_ENV}"
          printf 'EDGE_CONTAINERD_VERSION=%s\n' "${EDGE_CONTAINERD_VERSION}" >> "${GITHUB_ENV}"
          printf 'EDGE_COMPOSE_VERSION=%s\n' "${EDGE_COMPOSE_VERSION}" >> "${GITHUB_ENV}"
          agent_manifest="${RUNNER_TEMP}/edge-agent-artifact.json"
          jq -n \
            --arg source_revision "${EDGE_RUNTIME_SOURCE_REVISION}" \
            --arg sha256 "${EDGE_AGENT_SHA256}" \
            '{schema:1,source_revision:$source_revision,sha256:$sha256}' \
            > "${agent_manifest}"
          chmod 600 "${agent_manifest}"
          printf 'EDGE_APPLICATION_ORCHESTRATOR=%s\n' "${orchestrator}" >> "${GITHUB_ENV}"
          printf 'EDGE_RELEASE_CONTEXT_PATH=%s\n' "${dir}/resolved.env" >> "${GITHUB_ENV}"
          printf 'EDGE_APPLICATION_ARTIFACT=%s\n' "${agent}" >> "${GITHUB_ENV}"
          printf 'EDGE_APPLICATION_ARTIFACT_MANIFEST=%s\n' "${agent_manifest}" >> "${GITHUB_ENV}"

      - name: Materialize strict SSH authority
        shell: bash
        env:
          VULTR_SSH_PRIVATE_KEY: ${{ secrets.VULTR_SSH_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          key_path="${RUNNER_TEMP}/singbox-vultr-ops"
          umask 077
          printf '%s\n' "${VULTR_SSH_PRIVATE_KEY}" > "${key_path}"
          test -s "${key_path}"
          chmod 600 "${key_path}"
          printf 'EDGE_SSH_PRIVATE_KEY_PATH=%s\n' "${key_path}" >> "${GITHUB_ENV}"

      - name: Discover controller egress IPv4
        shell: bash
        run: |
          set -euo pipefail
          ip="$(curl -4 --fail --silent --show-error --connect-timeout 10 --max-time 20 https://api.ipify.org)"
          python3 - "${ip}" <<'PY'
          import ipaddress
          import sys
          value = ipaddress.ip_address(sys.argv[1].strip())
          if value.version != 4 or not value.is_global:
              raise SystemExit("controller egress is not a global IPv4 address")
          print(value)
          PY
          printf 'EDGE_CONTROLLER_IPV4=%s\n' "${ip}" >> "${GITHUB_ENV}"

      - name: Execute typed application lifecycle operation
        shell: bash
        env:
          VULTR_API_KEY: ${{ secrets.VULTR_API_KEY }}
        run: |
          set -euo pipefail
          bin="${EDGE_APPLICATION_ORCHESTRATOR}"
          result="${RUNNER_TEMP}/application-result.json"
          access_acquire="${RUNNER_TEMP}/application-access-acquire.json"
          access_release="${RUNNER_TEMP}/application-access-release.json"
          vultr_spec="$(jq -er '.vultr_spec_path' "${SPEC_PATH}")"
          machine="$(jq -er '.machine_id' "${SPEC_PATH}")"
          test -f "${vultr_spec}"

          ACCESS_CLEANUP_ARMED=0

          cleanup_access() {
            if [[ "${ACCESS_CLEANUP_ARMED}" != "1" ]]; then
              return 0
            fi
            "${bin}" vultr-lifecycle lease-release "${vultr_spec}" "${machine}" \
              | tee "${access_release}"
          }

          finish() {
            local operation_rc=$?
            trap - EXIT
            set +e
            cleanup_access
            local cleanup_rc=$?
            set -e
            if [[ ${cleanup_rc} -ne 0 ]]; then
              echo "ephemeral SSH ingress cleanup failed with rc=${cleanup_rc}" >&2
              if [[ ${operation_rc} -eq 0 ]]; then
                operation_rc=${cleanup_rc}
              fi
            fi
            exit "${operation_rc}"
          }

          trap finish EXIT
          ACCESS_CLEANUP_ARMED=1

          "${bin}" vultr-lifecycle lease-acquire "${vultr_spec}" "${machine}" \
            | tee "${access_acquire}"
          jq -e '.status == "READY"' "${access_acquire}" >/dev/null

          case "${REQUESTED_OPERATION}" in
            plan|verify)
              "${bin}" application-lifecycle "${REQUESTED_OPERATION}" \
                "${SPEC_PATH}" "${EDGE_APPLICATION_ARTIFACT_MANIFEST}" "${EDGE_APPLICATION_ARTIFACT}" \
                | tee "${result}"
              ;;
            apply|upgrade)
              authority_plan="${RUNNER_TEMP}/application-authority-plan.json"
              "${bin}" application-lifecycle plan \
                "${SPEC_PATH}" "${EDGE_APPLICATION_ARTIFACT_MANIFEST}" "${EDGE_APPLICATION_ARTIFACT}" \
                > "${authority_plan}"
              authority_digest="$(jq -er '.plan_authority.authority_digest' "${authority_plan}")"
              [[ "${authority_digest}" =~ ^[0-9a-f]{64}$ ]]
              "${bin}" application-lifecycle "${REQUESTED_OPERATION}" \
                "${SPEC_PATH}" "${EDGE_APPLICATION_ARTIFACT_MANIFEST}" "${EDGE_APPLICATION_ARTIFACT}" \
                "${authority_digest}" | tee "${result}"
              ;;
            recover-plan)
              "${bin}" application-lifecycle recover-plan "${SPEC_PATH}" | tee "${result}"
              ;;
            recover-apply)
              [[ "${RECOVERY_AUTHORITY_DIGEST}" =~ ^[0-9a-f]{64}$ ]]
              "${bin}" application-lifecycle recover-apply \
                "${SPEC_PATH}" "${RECOVERY_AUTHORITY_DIGEST}" | tee "${result}"
              ;;
            rollback-plan)
              "${bin}" application-lifecycle rollback-plan "${SPEC_PATH}" | tee "${result}"
              ;;
            rollback-apply)
              rollback_plan="${RUNNER_TEMP}/application-rollback-authority-plan.json"
              "${bin}" application-lifecycle rollback-plan "${SPEC_PATH}" > "${rollback_plan}"
              planned_rollback_digest="$(jq -er '.rollback.rollback_digest' "${rollback_plan}")"
              authority_digest="$(jq -er '.plan_authority.authority_digest' "${rollback_plan}")"
              test "${planned_rollback_digest}" = "${ROLLBACK_DIGEST}"
              [[ "${authority_digest}" =~ ^[0-9a-f]{64}$ ]]
              "${bin}" application-lifecycle rollback-apply \
                "${SPEC_PATH}" "${ROLLBACK_DIGEST}" "${authority_digest}" | tee "${result}"
              ;;
            *)
              echo "unsupported authorized application operation" >&2
              exit 1
              ;;
          esac

          {
            echo '### VM application lifecycle result'
            echo
            echo 'Ephemeral SSH ingress was acquired through the Vultr support-resource authority.'
            echo 'The EXIT trap releases the exact runner /32 and fails the job if absence is not verified.'
            echo
            echo '~~~json'
            cat "${result}"
            echo '~~~'
          } >> "${GITHUB_STEP_SUMMARY}"

  cleanup:
    needs: authorize
    concurrency:
      group: vultr-control-plane-production
      cancel-in-progress: false
    if: needs.authorize.outputs.operation == 'cleanup'
    runs-on: ubuntu-24.04
    environment:
      name: production-vultr
      deployment: false
    timeout-minutes: 45
    permissions:
      contents: read
    env:
      SPEC_PATH: ${{ needs.authorize.outputs.spec_path }}
      DNS_SPEC_PATH: infra/cloudflare/application-acceptance-dns.json
      MESH_SPEC_PATH: infra/cloudflare/application-acceptance-mesh.json
      VPC_SPEC_PATH: infra/vultr/application-acceptance-vpc.json

    steps:
      - name: Checkout exact canonical revision
        shell: bash
        env:
          REPOSITORY: ${{ github.repository }}
          REVISION: ${{ github.sha }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "https://github.com/${REPOSITORY}.git"
          git fetch --no-tags --depth=1 origin "${REVISION}"
          git checkout --detach FETCH_HEAD
          test "$(git rev-parse HEAD)" = "${REVISION}"
          test -f "${SPEC_PATH}"
          test -f "${DNS_SPEC_PATH}"
          test -f "${MESH_SPEC_PATH}"
          test -f "${VPC_SPEC_PATH}"

      - name: Resolve exact durable Edge Platform release once
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          ACCEPTED_REVISION: ${{ github.sha }}
          OUTPUT_DIR: ${{ runner.temp }}/edge-platform-release
        run: |
          set -euo pipefail
          bash edge-platform/scripts/resolve_durable_release.sh
          dir="${RUNNER_TEMP}/edge-platform-release"
          source "${dir}/resolved.env"
          test "${EDGE_ACCEPTED_REVISION}" = "${GITHUB_SHA}"
          test "${EDGE_RELEASE_TAG}" = "edge-release-${EDGE_RELEASE_SET_SHA256}"
          orchestrator="${dir}/edge-orchestrator-linux-amd64"
          test "$(sha256sum "${orchestrator}" | awk '{print $1}')" = "${EDGE_ORCHESTRATOR_SHA256}"
          chmod 0755 "${orchestrator}"
          printf 'EDGE_APPLICATION_ORCHESTRATOR=%s\n' "${orchestrator}" >> "${GITHUB_ENV}"
          printf 'EDGE_RELEASE_CONTEXT_PATH=%s\n' "${dir}/resolved.env" >> "${GITHUB_ENV}"

      - name: Validate disposable cleanup authority
        shell: bash
        run: |
          set -euo pipefail
          test "$(jq -er '.environment' "${SPEC_PATH}")" = "application-acceptance"
          test "$(jq -er '.machine_id' "${SPEC_PATH}")" = "application-acceptance-1"
          test "$(jq -er '.record_name' "${DNS_SPEC_PATH}")" = "stage2-acceptance.alegria.by"
          test "$(jq -er '.environment' "${VPC_SPEC_PATH}")" = "application-acceptance"
          test "$(jq -er '.machine_id' "${VPC_SPEC_PATH}")" = "application-acceptance-1"
          if grep -R --fixed-strings -n "miu.alegria.by" \
              "${MESH_SPEC_PATH}" "${DNS_SPEC_PATH}" "${VPC_SPEC_PATH}" "${SPEC_PATH}"; then
            echo "disposable cleanup must not reference permanent production DNS" >&2
            exit 1
          fi

      - name: Run typed disposable cleanup coordinator
        shell: bash
        env:
          VULTR_API_KEY: ${{ secrets.VULTR_API_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          set -euo pipefail
          result="${RUNNER_TEMP}/application-cleanup-result.json"
          set +e
          "${EDGE_APPLICATION_ORCHESTRATOR}" application-cleanup \
            "${SPEC_PATH}" \
            "${DNS_SPEC_PATH}" \
            "${MESH_SPEC_PATH}" \
            "${VPC_SPEC_PATH}" \
            | tee "${result}"
          operation_rc="${PIPESTATUS[0]}"
          set -e

          if [[ -s "${result}" ]]; then
            {
              echo '### Disposable application cleanup'
              echo '~~~json'
              cat "${result}"
              echo '~~~'
            } >> "${GITHUB_STEP_SUMMARY}"
          fi

          exit "${operation_rc}"


  acceptance:
    needs: authorize
    concurrency:
      group: vultr-control-plane-production
      cancel-in-progress: false
    if: needs.authorize.outputs.operation == 'acceptance'
    runs-on: ubuntu-24.04
    environment:
      name: production-vultr
      deployment: false
    timeout-minutes: 90
    permissions:
      contents: read
    env:
      SPEC_PATH: ${{ needs.authorize.outputs.spec_path }}

    steps:
      - name: Checkout exact canonical revision
        shell: bash
        env:
          REPOSITORY: ${{ github.repository }}
          REVISION: ${{ github.sha }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "https://github.com/${REPOSITORY}.git"
          git fetch --no-tags --depth=1 origin "${REVISION}"
          git checkout --detach FETCH_HEAD
          test "$(git rev-parse HEAD)" = "${REVISION}"
          test -f "${SPEC_PATH}"

      - name: Resolve exact durable Edge Platform release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          ACCEPTED_REVISION: ${{ github.sha }}
          OUTPUT_DIR: ${{ runner.temp }}/edge-platform-release
        run: |
          set -euo pipefail
          bash edge-platform/scripts/resolve_durable_release.sh

      - name: Materialize verified application artifacts
        shell: bash
        run: |
          set -euo pipefail
          dir="${RUNNER_TEMP}/edge-platform-release"
          source "${dir}/resolved.env"
          test "${EDGE_ACCEPTED_REVISION}" = "${GITHUB_SHA}"
          test "${EDGE_RELEASE_TAG}" = "edge-release-${EDGE_RELEASE_SET_SHA256}"
          test "${EDGE_RELEASE_SCHEMA_VERSION}" = "4"
          [[ "${EDGE_RUNTIME_SOURCE_REVISION}" =~ ^[0-9a-f]{40}$ ]]
          [[ "${EDGE_RUNTIME_INPUT_SHA256}" =~ ^[0-9a-f]{64}$ ]]
          orchestrator="${dir}/edge-orchestrator-linux-amd64"
          agent="${dir}/edge-agent-linux-amd64"
          test "$(sha256sum "${orchestrator}" | awk '{print $1}')" = "${EDGE_ORCHESTRATOR_SHA256}"
          test "$(sha256sum "${agent}" | awk '{print $1}')" = "${EDGE_AGENT_SHA256}"
          gateway_image="${EDGE_GATEWAY_IMAGE}"
          warp_image="${EDGE_WARP_EGRESS_IMAGE}"
          mesh_image="${EDGE_MESH_IMAGE}"
          [[ "${gateway_image}" =~ ^ghcr\.io/iamaman11/vultr-edge-gateway@sha256:[0-9a-f]{64}$ ]]
          [[ "${warp_image}" =~ ^ghcr\.io/iamaman11/vultr-warp-egress@sha256:[0-9a-f]{64}$ ]]
          [[ "${mesh_image}" =~ ^docker\.io/cloudflare/mesh@sha256:[0-9a-f]{64}$ ]]
          bundle_root="$(jq -er '.bundle_root' "${SPEC_PATH}")"
          test -d "${bundle_root}"
          printf 'EDGE_GATEWAY_IMAGE=%s\nEDGE_WARP_EGRESS_IMAGE=%s\nCLOUDFLARE_MESH_IMAGE=%s\n' \
            "${gateway_image}" "${warp_image}" "${mesh_image}" > "${bundle_root}/.images.env"
          chmod 0644 "${bundle_root}/.images.env"
          chmod 0755 "${orchestrator}" "${agent}"
          printf 'EDGE_DOCKER_ENGINE_VERSION=%s\n' "${EDGE_DOCKER_ENGINE_VERSION}" >> "${GITHUB_ENV}"
          printf 'EDGE_CONTAINERD_VERSION=%s\n' "${EDGE_CONTAINERD_VERSION}" >> "${GITHUB_ENV}"
          printf 'EDGE_COMPOSE_VERSION=%s\n' "${EDGE_COMPOSE_VERSION}" >> "${GITHUB_ENV}"
          agent_manifest="${RUNNER_TEMP}/edge-agent-artifact.json"
          jq -n \
            --arg source_revision "${EDGE_RUNTIME_SOURCE_REVISION}" \
            --arg sha256 "${EDGE_AGENT_SHA256}" \
            '{schema:1,source_revision:$source_revision,sha256:$sha256}' \
            > "${agent_manifest}"
          chmod 600 "${agent_manifest}"
          printf 'EDGE_APPLICATION_ORCHESTRATOR=%s\n' "${orchestrator}" >> "${GITHUB_ENV}"
          printf 'EDGE_RELEASE_CONTEXT_PATH=%s\n' "${dir}/resolved.env" >> "${GITHUB_ENV}"
          printf 'EDGE_APPLICATION_ARTIFACT=%s\n' "${agent}" >> "${GITHUB_ENV}"
          printf 'EDGE_APPLICATION_ARTIFACT_MANIFEST=%s\n' "${agent_manifest}" >> "${GITHUB_ENV}"

      - name: Materialize strict SSH authority
        shell: bash
        env:
          VULTR_SSH_PRIVATE_KEY: ${{ secrets.VULTR_SSH_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          key_path="${RUNNER_TEMP}/singbox-vultr-ops"
          umask 077
          printf '%s\n' "${VULTR_SSH_PRIVATE_KEY}" > "${key_path}"
          test -s "${key_path}"
          chmod 600 "${key_path}"
          printf 'EDGE_SSH_PRIVATE_KEY_PATH=%s\n' "${key_path}" >> "${GITHUB_ENV}"

      - name: Verify provider authorities before acceptance
        shell: bash
        env:
          VULTR_API_KEY: ${{ secrets.VULTR_API_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ENROLLED_DEVICE_REACHABILITY_CONFIRMED: ${{ vars.CLOUDFLARE_ENROLLED_DEVICE_REACHABILITY_CONFIRMED }}
        run: |
          set -euo pipefail
          test -n "${VULTR_API_KEY}"
          test -n "${CLOUDFLARE_API_TOKEN}"
          test -s "${EDGE_SSH_PRIVATE_KEY_PATH}"

          bin="${EDGE_APPLICATION_ORCHESTRATOR}"
          vultr_spec="$(jq -er '.vultr_spec_path' "${SPEC_PATH}")"
          test -f "${vultr_spec}"

          "${bin}" vultr-lifecycle doctor "${vultr_spec}"             > "${RUNNER_TEMP}/acceptance-vultr-authority-doctor.json"
          jq -e '
            .status == "PASS" and
            .catalog_validation == "PASS" and
            .operator_key_match == "PASS" and
            .mutations_performed == 0
          ' "${RUNNER_TEMP}/acceptance-vultr-authority-doctor.json" >/dev/null

          "${bin}" cloudflare-zero-trust doctor             infra/cloudflare/zero-trust-guardrails.json             > "${RUNNER_TEMP}/acceptance-zero-trust-doctor.json"
          jq -e '
            .status == "PASS" and
            .token_read_authority == "PASS" and
            .device_settings.ready == true and
            .connectors.ready == true and
            .mesh_node_profile.ready == true and
            .enrollment.ready == true and
            .gateway.ready == true and
            .manual_prerequisites.ready == true
          ' "${RUNNER_TEMP}/acceptance-zero-trust-doctor.json" >/dev/null

      - name: Validate disposable provider desired state
        shell: bash
        run: |
          set -euo pipefail
          mesh_base_spec="infra/cloudflare/application-acceptance-mesh.json"
          dns_spec="infra/cloudflare/application-acceptance-dns.json"
          vpc_spec="infra/vultr/application-acceptance-vpc.json"
          test -f "${mesh_base_spec}"
          test -f "${dns_spec}"
          test -f "${vpc_spec}"
          test "$(jq -er '.account_id' "${mesh_base_spec}")" = "4426df1449e417511bc7697d60b7f62f"
          test "$(jq -er '.routes | length' "${mesh_base_spec}")" = "0"
          test "$(jq -er '.record_name' "${dns_spec}")" = "stage2-acceptance.alegria.by"
          test "$(jq -er '.zone_name' "${dns_spec}")" = "alegria.by"
          test "$(jq -er '.environment' "${vpc_spec}")" = "application-acceptance"
          test "$(jq -er '.machine_id' "${vpc_spec}")" = "application-acceptance-1"
          if grep -R --fixed-strings -n "miu.alegria.by" \
              "${mesh_base_spec}" "${dns_spec}" "${vpc_spec}" "${SPEC_PATH}"; then
            echo "disposable acceptance must not reference permanent production DNS" >&2
            exit 1
          fi
          printf 'EDGE_ACCEPTANCE_MESH_BASE_SPEC=%s\n' "${mesh_base_spec}" >> "${GITHUB_ENV}"
          printf 'EDGE_ACCEPTANCE_DNS_SPEC=%s\n' "${dns_spec}" >> "${GITHUB_ENV}"
          printf 'EDGE_ACCEPTANCE_VPC_SPEC=%s\n' "${vpc_spec}" >> "${GITHUB_ENV}"

      - name: Discover controller egress IPv4
        shell: bash
        run: |
          set -euo pipefail
          ip="$(curl -4 --fail --silent --show-error --connect-timeout 10 --max-time 20 https://api.ipify.org)"
          python3 - "${ip}" <<'PY'
          import ipaddress
          import sys
          value = ipaddress.ip_address(sys.argv[1].strip())
          if value.version != 4 or not value.is_global:
              raise SystemExit("controller egress is not a global IPv4 address")
          print(value)
          PY
          printf 'EDGE_CONTROLLER_IPV4=%s\n' "${ip}" >> "${GITHUB_ENV}"

      - name: Run typed provider-backed disposable acceptance
        shell: bash
        env:
          VULTR_API_KEY: ${{ secrets.VULTR_API_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          set -euo pipefail
          result="${RUNNER_TEMP}/application-acceptance-result.json"

          set +e
          "${EDGE_APPLICATION_ORCHESTRATOR}" application-acceptance \
            "${SPEC_PATH}" \
            "${EDGE_APPLICATION_ARTIFACT_MANIFEST}" \
            "${EDGE_APPLICATION_ARTIFACT}" \
            "${EDGE_ACCEPTANCE_DNS_SPEC}" \
            "${EDGE_ACCEPTANCE_MESH_BASE_SPEC}" \
            "${EDGE_ACCEPTANCE_VPC_SPEC}" \
            | tee "${result}"
          operation_rc="${PIPESTATUS[0]}"
          set -e

          if [[ -s "${result}" ]]; then
            {
              echo '### Full disposable Stage 2 application acceptance'
              echo '~~~json'
              cat "${result}"
              echo '~~~'
            } >> "${GITHUB_STEP_SUMMARY}"
          fi

          exit "${operation_rc}"

      - name: Remove local sensitive material
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          rm -f "${EDGE_SSH_PRIVATE_KEY_PATH:-${RUNNER_TEMP}/singbox-vultr-ops}"
